services:
  liste-de-course:
    build: .
    container_name: liste-de-course
    restart: unless-stopped
    ports:
      - "3030:3030"
    volumes:
      - liste-data:/data
    environment:
      - PORT=3030
      - DATA_DIR=/data

volumes:
  liste-data: